---
import "../styles/global.css";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import WhatsAppButton from "../components/WhatsAppButton.astro";
import PwaToast from "../components/PwaToast.astro";
import PwaInstallButton from "../components/PwaInstallButton.astro";
import SEO from "../components/SEO.astro";
import type { SEOProps } from "../lib/seo";

export interface Props {
  seo?: SEOProps;
  title?: string;
  description?: string;
  image?: string;
  preloadHero?: boolean;
}

const { seo, title, description, image, preloadHero } = Astro.props;
const seoProps = { ...seo, title, description, image };
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="light dark" />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <link rel="apple-touch-icon" href={`${import.meta.env.BASE_URL}apple-touch-icon.png`} />
    <link rel="manifest" href={`${import.meta.env.BASE_URL}manifest.json`} />
    <meta name="theme-color" content="#14213d" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="ZN Enterprises" />
    {preloadHero && <link rel="preload" as="image" href={`${import.meta.env.BASE_URL}images/hero-bg.jpeg`} />}
    <link rel="preload" as="font" type="font/woff2" crossorigin href={`${import.meta.env.BASE_URL}fonts/inter-latin.woff2`} />
    <SEO {...seoProps} />
    <script is:inline>
      (function () {
        const theme = localStorage.getItem("theme");
        if (theme !== "light") {
          document.documentElement.classList.add("dark");
        }
      })();
    </script>
  </head>
  <body class="flex flex-col min-h-screen bg-page text-body font-sans antialiased scrollbar-thin scrollbar-thumb-accent/50 scrollbar-track-transparent">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-accent focus:text-accent-text focus:rounded-xl focus:outline-none focus:shadow-lg">
      Skip to main content
    </a>
    <Navbar />
    <main id="main-content" class="flex-1">
      <slot />
    </main>
    <Footer />
    <PwaToast />
    <PwaInstallButton />
    <WhatsAppButton />

    <script>
      if ("serviceWorker" in navigator) {
        window.addEventListener("load", () => {
          navigator.serviceWorker.register(`${import.meta.env.BASE_URL}sw.js`);
        });
      }
    </script>
  </body>
</html>
